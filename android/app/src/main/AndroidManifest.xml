<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- usesCleartextTraffic: a app fala HTTP/WS com o servidor na rede
         LOCAL indicada pelo utilizador (pairing). Sem isto, o Android 9+
         bloqueia todo o tráfego http:// com "CLEARTEXT not permitted" e
         nada funciona, mesmo com a rede OK. Postura: LAN-only; TLS fica
         para a v2 (ver README, secção Segurança). -->
    <application
        android:label="FlowTools"
        android:theme="@style/Theme.FlowTools"
        android:allowBackup="false"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
