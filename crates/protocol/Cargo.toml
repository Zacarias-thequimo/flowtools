[package]
name = "flowtools-protocol"
version = "0.1.0"
edition = "2024"
description = "FlowTools shared protocol contract (session, events, errors)"
license = "MIT"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
